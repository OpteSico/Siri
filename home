<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>暗号の家 - オープニング</title>
  <style>
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background-color: #0d0d0d;
      color: white;
      overflow: hidden;
    }
    .screen {
      display: none;
      height: 100vh;
      width: 100vw;
      position: absolute;
      top: 0; left: 0;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      transition: opacity 1s;
    }
    .visible {
      display: flex;
      opacity: 1;
    }
    .fade-out {
      opacity: 0;
    }
    h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      animation: fadeIn 2s ease;
    }
    p {
      font-size: 1.1rem;
      max-width: 80%;
      text-align: center;
      margin-bottom: 2rem;
      animation: fadeIn 3s ease;
    }
    button {
      padding: 0.8rem 2rem;
      margin: 0.5rem;
      background-color: #1a1a1a;
      border: 1px solid #555;
      color: white;
      font-size: 1rem;
      border-radius: 5px;
      cursor: pointer;
    }
    button:hover {
      background-color: #333;
    }
    @keyframes fadeIn {
      from {opacity: 0; transform: translateY(20px);}
      to {opacity: 1; transform: translateY(0);}
    }
  </style>
</head>
<body>
  <!-- オープニング画面 -->
  <div id="introScreen" class="screen visible">
    <h1>暗号の家</h1>
    <p>あなたは記憶を失った状態で目覚め、暗号のかかった扉の前に立っていた。<br>4桁の数字を解き明かして、この家から脱出せよ。</p>
    <button onclick="goToMenu()">スタート</button>
    <button onclick="skipToMenu()">スキップ</button>
  </div>

  <!-- メニュー画面 -->
  <div id="menuScreen" class="screen">
    <h1>暗号の家 - メニュー</h1>
    <button onclick="startGame()">新しく始める</button>
    <button onclick="howToPlay()">遊び方</button>
    <button onclick="openCredits()">クレジット</button>
  </div>

  <script>
    function goToMenu() {
      const intro = document.getElementById('introScreen');
      intro.classList.add('fade-out');
      setTimeout(() => {
        intro.classList.remove('visible');
        document.getElementById('menuScreen').classList.add('visible');
      }, 1000);
    }

    function skipToMenu() {
      document.getElementById('introScreen').classList.remove('visible');
      document.getElementById('menuScreen').classList.add('visible');
    }

    function startGame() {
      alert("ゲームスタート！（次の画面はまだ未実装）");
    }

    function howToPlay() {
      alert("4桁の暗号をNPCの証言を元に推理してください。");
    }

    function openCredits() {
      alert("制作：あなたの名前またはチーム名");
    }
  </script>
</body>
</html>
